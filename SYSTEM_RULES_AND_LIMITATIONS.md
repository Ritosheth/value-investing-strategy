# 股票投资研究系统：规则、数据口径与局限

版本口径：2026-08-01 修复版。本文用于审计系统如何选股、如何估值，以及哪些输出不能直接视为投资结论。

## 一、运行链路

1. 启动器调用富途 V2 全市场筛选，读取中国市场的实时原始因子。
2. 对候选股执行风险、流动性、流通市值和上市时间门槛。
3. 基于当次原始因子重新计算质量、成长、估值和量价分，不默认信任 CSV 中保存的旧评分。
4. 三个模型分别生成质量成长、行业资金质量、事件资金确认名单。
5. 指定股票进入深研时，无论它是否在全市场榜单前列，都会单独进入本地模型和富途证据采集。
6. 深研生成相对估值、FCFF DCF、证据评分、姿态和影子仓位，并将决策快照写入 `outputs/deep_research/research_history.csv`。

系统只读行情和研究数据，不下单、不自动修改实盘仓位。

## 二、候选池和硬门槛

- 市场：CN。
- 实时候选上限：120，只是计算批次上限，不表示全市场只有 120 只。
- 排序：富途近 20 日平均成交额降序。
- 近 20 日平均成交额：不低于 5,000 万元。
- 流通市值：不低于 30 亿元。
- 上市时间：不少于 120 天，降低新股短期同比和筹码异常的干扰。
- 名称或风险字段包含 `ST`、`*ST`、`退`：直接剔除。
- 富途全市场筛选不可用时：降级到配置 CSV，并在元数据中标记 `configured_seed_universe`，不能把降级结果描述为全市场选股。

## 三、基础评分

以下分数均限制在 0–100：

- 基本面质量分 = 35 + 1.5×ROE + 0.08×净利润现金含量 - 0.10×资产负债率。现金含量计算前限制在 -100% 至 200%。
- 成长质量分 = 50 + 营收增长率；营收增长率缺失时，降级为 35 + 0.5×20 日涨跌幅，并标记 `partial_raw_proxy`。
- 估值分 = 100 - 估值分位。估值分位 = 65%×PE 横截面分位 + 35%×PB 横截面分位。样本少于 5 只时使用 50 分中性值，防止单只股票得到错误的 0 分或 100 分。
- 量价分 = 40 + 20 日涨跌幅 + 30×成交额横截面分位。
- 特征覆盖率：ROE、营收增长、PE、PB、20 日涨跌幅、成交额六项原始特征的可用比例。

基础质量总分 = 35%基本面 + 35%成长 + 20%估值 + 10%量价；达到 65 分进入 `core`，否则为 `satellite`。`core` 只是研究分层，不是买入指令。

## 四、三个选股模型

### 模型一：质量成长

- 总分 = 32%基本面 + 33%成长 + 22%估值 + 10%量价 + 3%股东回报。
- 股东回报来自分红/回购证据。
- 名单优先展示 `core`，再按总分排序。

### 模型二：行业资金质量

- 个股质量 = 45%基本面 + 35%成长 + 20%估值。
- 预筛分 = 65%个股质量 + 35%量价。
- 资金流分 = 45%近 20 日资金强度分位 + 35%资金净流入天数比例 + 20%大单净流入分位。
- 行业原始强度 = 55%行业内平均资金流分 + 45%行业内平均量价分。
- 行业样本少于 3 只时，行业强度按样本覆盖率向 50 分收缩，单只股票不能再直接代表整个行业。
- 总分 = 55%个股质量 + 30%行业强度 + 10%资金流 + 5%量价。

### 模型三：事件与资金确认

- 个股质量 = 45%基本面 + 35%成长 + 20%估值。
- 催化分 = 45%事件分 + 40%预期分 + 15%股东回报分。
- 总分 = 35%催化 + 30%资金流 + 25%个股质量 + 10%量价。
- 硬确认：10 日或 20 日主力净流入必须大于 0；缺少资金流数据不会获得中性分。

## 五、轮动和入场位置

- 轮动龙头分 = 35%行业强度 + 35%资金流 + 30%量价。
- 轮动持续性分 = 45%行业强度 + 25%资金加速度 + 30%基本面。
- 轮动热度分 = 60%量价 + 40%资金加速度。
- 入场位置分 = 40%估值 + 35%量价 + 25%×(100-轮动热度)。

入场分达到 68 为“较适合研究建仓区”，58–68 为“筑底观察”，48–58 为“改善观察”，低于 48 为“偏高或不清晰”。这些状态不替代止损、组合约束和交易执行规则。

## 六、深研评分和估值约束

深研权重：模型 15%、财务质量 20%、估值 15%、催化 15%、技术/资金 15%、治理风险 15%、数据置信度 5%。财务、估值、技术资金、治理为关键组件；缺失时结论为 `INSUFFICIENT EVIDENCE`。

- 财务质量优先从最新财务报表的 ROE、净利率、净利润现金含量、3 年营收/利润增长和资产负债率推导；报表指标不足才降级使用本地模型分。
- 估值分优先使用富途历史估值分位的反向得分；不可用时才降级到本地横截面估值分。
- 治理分只是证据覆盖度筛查，最高 60 分；不能解释为治理质量已验证。ST/退市风险会触发低分和硬限制。
- 相对估值至少需要 30 个历史样本才可约束决策。
- DCF 只有在模型标记为 `decision_usable` 时才可约束决策；低置信度现金流底值不会机械否决股票。
- 所有可用估值锚的基准上行空间均不高于 -20% 时，姿态强制降为 `REJECT-RISK WATCH`，影子仓位为 0%。
- 两种估值方法相差 50 个百分点以上时，置信度降为 LOW，仓位上限相应收缩。
- DCF 显式预测期随研究周期变化：短期 3 年、中期 5 年、长期 7 年。
- 保守风险偏好会把影子仓位上限下调一档；不会因为“激进”偏好而放松硬风险限制。

## 七、仍需人工或后续系统补齐的事项

- 治理：目前没有自动覆盖全部监管处罚、诉讼、关联交易、质押和审计意见，必须结合交易所公告核验。
- 催化：富途证据没有未来业绩日或公告日时，系统会明确显示缺口，不会虚构日期。
- 组合：多股报告计算行业集中度；由于证据包尚未保存统一日期对齐的全量收益序列，不输出伪精确相关系数。建仓前应另做相关性、回撤和压力测试。
- 业绩验证：系统保存每次研究时点、股价、姿态和估值锚，但尚未自动计算 1/3/6/12 个月超额收益、交易成本和基准收益。
- 收益复盘：可运行 `deep-stock-research/scripts/evaluate_research_history.py`，用富途当前价计算各历史研究时点至今的前瞻收益；基准收益和超额收益仍保留为空，避免在没有基准序列时伪造结果。
- 模型有效性：当前权重是规则参数，不是已证明能产生超额收益的结论。应在足够长的滚动样本上做样本外回测、分层收益和阈值校准后再提高资金使用权重。
